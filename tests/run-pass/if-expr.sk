fn bar(x: int) {
	print "starting";
	let z = null;
	if x == 2 {
		return "a";
	} else {
		z = x;
	}
	if z + x == z {
		print "zero";
		return 0;
	}
	print "here";
	 if z==10 { return 8; } else { print z; }
}

fn main() {
	print bar(2);
	print bar(0);
	print bar(1);
	print bar(10);
}
